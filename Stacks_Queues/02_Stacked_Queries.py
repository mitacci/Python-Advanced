n = int(imput())
stack = []
for _ in range(n):
  line_parts = input().split()
  command = line_parts[0]
  if command == "1":
    number = int(line_parts[1]
    stack.append(number)
  elif command == "2":
                 
                 
              
